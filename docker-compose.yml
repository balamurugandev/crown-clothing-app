version: '3.4'

services:
  vite:
    image: vite
    build:
      context: node_modules/vite
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
